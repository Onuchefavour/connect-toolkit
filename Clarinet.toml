[project]
name = "connect-toolkit"
authors = ["Connect Toolkit Team"]
description = "A blockchain-powered learning milestone and achievement tracking system"
telemetry = false
requirements = []
[contracts.connect-learning]
path = "contracts/connect-learning.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
